#include <stdio.h>

int main() {
     
     float a,b;
     float s , p,m,c;
     printf("entre la valeur de a:");
     scanf("%f",&a);
     printf("entre la valeur de b:");
     scanf("%f",&b);
     s = a +b;
     p= a-b;
     m= a*b;
     c= a/b;
     printf("s = %.2f\n",s);
     printf("p = %.2f\n",p);
     printf("m = %.2f\n",m);
     printf("c = %.2f\n",c);
         
     
       
     
         
     
     
     

    return 0;
}